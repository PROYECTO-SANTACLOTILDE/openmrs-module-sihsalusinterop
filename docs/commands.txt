# Borrar Módulo del Docker
docker exec peruHCE-backend rm /openmrs/data/modules/sihsalusinterop-1.0.0.omod

# Reiniciar
docker restart peruHCE-backend

# Compilar el Módulo Completo
D:\Johan\Documentos\Projects\sihsalus\sihsalus-distro-referenceapplication
mvn clean package

# Inyectar en Docker
docker cp omod/target/sihsalusinterop-1.0.0.omod peruHCE-backend:/openmrs/data/modules/

# Reiniciar
docker restart peruHCE-backend

# Ver logs
docker logs -f --tail 10 peruHCE-backend